- 기존에 개발하던 메인과는 분리된 BT용 build, 도메인을 분리 생성하기 위한 과정

- 도메인 생성
- default.conf -> uptream app 내용을 새로운 BT 서버에 맞게 변경
- docker-compose.yml 의 현재 패키지 서비스 이름을 중복되지 않게 변경
	- 포트 번호도 기존 메인과는 중복되지 않도록 변경
	- 이미지 이름도 중복되지 않게 변경
	- ( docker-compose.yml 에서 서비스 이름과 포트 번호, 이미지 이름만 변경한다면 해당하는 새로운 도커 이미지가 생성되기 때문에 기존 프로젝트와는 중복되지 않는 BT용 이미지를 얻을 수 있다. )

**새로운 Docker 이미지 생성**